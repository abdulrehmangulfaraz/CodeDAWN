{
  "name": "codedawn",
  "displayName": "CodeDawn",
  "description": "A discreet, powerful, and context-aware AI code assistant.",
  "version": "2.0.0",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:codedawn.invoke",
    "onCommand:codedawn.openTerminal"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "codedawn.invoke",
        "title": "CodeDawn: Invoke with Input Box"
      },
      {
        "command": "codedawn.openTerminal",
        "title": "CodeDawn: Invoke with Terminal Input"
      }
    ],
    "keybindings": [
      {
        "command": "codedawn.invoke",
        "key": "ctrl+l",
        "mac": "cmd+l",
        "when": "editorTextFocus"
      },
      {
        "command": "codedawn.openTerminal",
        "key": "ctrl+shift+l",
        "mac": "cmd+shift+l",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "eslint": "^8.56.0",
    "vscode-test": "^1.6.1"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^2.7.0"
  }
}